services:
  pgadmin:
    image: dpage/pgadmin4:9.11.0
    container_name: pgadmin
    hostname: pgadmin
    env_file:
      - ./pgadmin/.env
    ports:
      - "${PGADMIN_PUBLISH_PORT}:${PGADMIN_DEFAULT_PORT}"
    volumes:
      - pgadmin_data:/var/lib/pgadmin
    networks:
      - docker-dev-common
    restart: no

volumes:
  pgadmin_data:

networks:
  docker-dev-common:
    external: true